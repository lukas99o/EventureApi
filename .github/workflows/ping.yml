name: Ping API

on:
  schedule:
    - cron: "*/5 * * * *" # var 5:e minut

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -I https://friendship-c3cfdgejf5ateyc2.swedencentral-01.azurewebsites.net/ping
